spring:
  application:
    name: study-spring-reactive
  jpa:
    hibernate:
      ddl-auto: create
  docker:
    compose:
      stop:
        command: down
        timeout: 1m
      skip:
        in-tests: false

---

spring:
  config:
    activate:
      on-profile: prod
  docker:
    compose:
      enabled: false

---

logging:
  level:
    root: debug
